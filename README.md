# 🏖️ Система планирования отпусков

> **Прозрачность, контроль и предсказуемость — ключ к здоровому рабочему процессу**

---

## Введение

В компаниях среднего размера процесс согласования отпусков часто превращается в хаотичную рутину: заявления в почте, Excel-таблицы, потеря информации и непреднамеренные пересечения отсутствий. Это не только вызывает напряжение в коллективе, но и ставит под угрозу выполнение ключевых проектов — когда сразу несколько сотрудников оказываются в отпуске, рабочий процесс останавливается.

**Цель проекта** — создать единый, прозрачный и удобный инструмент для планирования отпусков, в котором каждый участник — от рядового сотрудника до HR-специалиста — получает именно ту информацию и те права, которые ему необходимы.

Результатом стал веб-сервис, сочетающий простоту использования, надёжность хранения данных и гибкость настройки под нужды организации.

---

## 🚀 Быстрый старт

### Предварительные требования
- Docker и Docker Compose установлены
- Git для клонирования репозитория
- Порты 7055 и 27017 свободны

### Установка за 3 шага
1. **Клонируйте репозиторий:**
   ```bash
   git clone https://github.com/kamushkinael/project.git
   cd project

Настройте переменные окружения
```bash
cp backend/.env.example backend/.env
# Отредактируйте .env при необходимости
```

Запустите проект:
```bash
  docker-compose up --build
  ```
## ✅ Готово! Приложение доступно по адресу: http://localhost:7055 
### 2. **🔐 Раздел "Тестовые аккаунты"**
```markdown
## 👥 Тестовые аккаунты

После запуска seed-данных доступны следующие пользователи:

| Роль | Логин | Пароль | Описание |
|------|--------|---------|----------|
| HR | `hr_admin` | `password123` | Полный доступ ко всем функциям |
| Менеджер разработки | `dev_manager` | `password123` | Управляет отделом разработки |
| Менеджер продаж | `sales_manager` | `password123` | Управляет отделом продаж |
| Сотрудник | `developer1` | `password123` | Обычный сотрудник отдела разработки |
| Сотрудник | `sales1` | `password123` | Обычный сотрудник отдела продаж |
```
## Основные концепции проекта

### 1. **Ролевая модель доступа**

Система поддерживает три ключевые роли, каждая со своим функционалом:

- **Сотрудник**  
  Может подавать заявки на отпуск, отслеживать их статус и видеть календарь отсутствий в своём отделе.

- **Руководитель**  
  Получает уведомления о новых заявках, может согласовывать или отклонять их, а также контролировать общую загрузку отдела через визуализацию отпусков.

- **HR-специалист**  
  Управляет остатками отпускных дней, формирует отчёты по всей компании и экспортирует данные для внешней аналитики.

### 2. **Прозрачность и предотвращение конфликтов**

- Каждый сотрудник видит, кто и когда уходит в отпуск — это снижает недовольство и улучшает планирование.
- Руководитель получает **предупреждения**, если одновременно в отпуск уходит слишком много сотрудников (ограничение задаётся на уровне отдела).
- Заявки проходят чёткий жизненный цикл: *на рассмотрении → согласовано / отклонено*, с возможностью добавить комментарий.

### 3. **Централизованное хранение данных**

Вся информация хранится в надёжной базе данных **MongoDB**, что обеспечивает:

- Гибкость схемы данных;
- Изоляция и безопасность (поскольку доступ только из backend контейнера);
- Управление состоянием;
- JSON-подобный формат документов

### 4. **Удобный пользовательский интерфейс**

- Интерактивный календарь на основе даёт наглядное представление о загрузке отдела.
- Статусы заявок выделены цветными метками.
- Все интерфейсы адаптированы под мобильные устройства.

### 5. **Лёгкое развёртывание и поддержка**

- Проект упакован в **Docker-контейнеры**, развёртывание занимает одну команду:  
  ```bash
  docker-compose up --build
  ```
- Доступ к приложению по адресу: http://localhost:7055

---

## Структура проекта

С помощью технологии контейниризации Docker в проекте были развернуты следующие сервисы:
1.  База данных MongoDB;
2.  Backend-приложение на FastAPI;
3.  Frontend-приложение на React;
4.  Web-сервер Nginx

### **Технологии:**

- **Backend:** Python, FastAPI, Motor/PyMongo, Pydantic;
- **Frontend:** HTML, React, Tailwind CSS, FullCalendar.js;
- **Контейниризация:** Docker, Docker Compose, .env;
- **База данных:** MongoDB;
- **Версионирование и координация:** Git, GitHub, README, workflow;

---

- ### **Backend:**

```text
.env                # Файл с переменными окружениями (пароли, ключи API, настройки БД)                 
Dockerfile          # Инструкции для сборки Docker-образа приложения
auth.py             # Модуль аутентификации и авторизации
email_service.py    # Сервис отправки email
models.py           # Модели базы данных
requirements.txt    # Зависимости Python
seed_data.py        # Скрипт для наполнения БД начальными данными
seed_test_data.py   # Скрипт для тестовых данных
server.py           # Главный файл приложения, точка входа
utils.py            # Модуль аутентификации и авторизации
```

- ### **Frontend:**

```text
public              # Статические файлы
src                 # Исходный код React приложения
.env                # Файл с переменными окружениями (API_URL, ключи и т.д.)
Dockerfile          # Сборка Docker-образа React приложения
craco.config.js     # Конфигурация для Create React App    
nginx.conf          # Конфигурация Nginx
package.json        # Зависимости npm/yarn и скрипты
tailwind.config.js  # Конфигурация Tailwind CSS (кастомизация дизайн-системы)
yarn.lock           # Фиксация версий зависимостей
```

- ### **README.md**

Полная документация проекта.

- ### **Docker-compose.yml**

Используется для управления всеми сервисами проекта. Содержит описание конфигурации контейнров, их зависимости и сетевое взаимодействие.

- ### **Design_guidelines.json**

Используется как конфигурационный файл для хранения дизайн-токенов, стилей и правил дизайн-системы.

- ### **Nginx.conf**

Конфигурационный файл, который используется как прокси-сервер и балансировщик нагрузки для веб-приложения.

- ### **.github/workflows**

Автоматизация процесса обновления проекта с использованием Git и GitHub. Позволяет организовать удобный рабочий процесс, при котором изменения в коде после коммита в репозиторий автоматически применяются на сервере без необходимости ручного обновления.

---

## **Использование пользователями:**

### 1. **Сотрудник**

После авторизации сотрудник видит главный пользовательский интерфейс веб-приложения: баланс отпуска, статусы заявок. При нажатии на кнопку "+ Новая заявка", всплывает окно формирования новой заявки на отпуск с необходимыми пунктами заполнения информацией. Также вкладка "Календарь" позволяет просматривать календарь отпусков отпусков своего отдела. Есть функция изменения фона (светлая/темная).
<img width="1554" height="764" alt="image" src="https://github.com/user-attachments/assets/c707a020-f30a-4035-a3a6-2627f8f44ad6" />

<img width="565" height="661" alt="image" src="https://github.com/user-attachments/assets/d7d6e99e-bc10-4f8c-95a1-33f4b823cf0f" />

<img width="1579" height="892" alt="image" src="https://github.com/user-attachments/assets/a4eae05e-c8bc-47dd-bd1e-059ba481d68e" />

### 2. **Руководитель**

Имеет дополнительную вкладку "Заявки", которая позволяет просматривать и согласовывать/отклонять заявки сотрудников. Во вкладке "Календарь" отображается календарь отдела с визуализацией всех отпусков.

<img width="1563" height="525" alt="image" src="https://github.com/user-attachments/assets/fa8075b8-7939-496d-b13e-8700b5cf2881" />

<img width="1515" height="799" alt="image" src="https://github.com/user-attachments/assets/05e1c545-4b5e-43be-af41-75117c5a0bc6" />

### 3. **HR**

Содержит также вкладку "Заявки", в которой он может просматривать заявки. Вкладка "HR" позволяет: управлять балансом дней отпуска сотрудников, создает отчет по отпускам за необходимый период (при нажатии на кнопку "Экспортировать CSV, на утройство автоматически скачивается таблица).

<img width="1599" height="808" alt="image" src="https://github.com/user-attachments/assets/e536717b-953c-4daf-90dc-b33e3fda29d7" />

> В разработке данного проекта принимали участие: Товпеко Глеб, Исмагулова Элина, Коньков Максим, Каширский Денис, Гущарин Владимир.


